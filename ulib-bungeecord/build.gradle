dependencies {
    compile project(':ulib-bungeecord-api')
    compile project(':ulib-minecraft')
    compileOnly coordsWaterfall
}

//shadowing
shadowJar {
    configurations = [project(':ulib-core').configurations.shade]
    //noinspection GroovyAssignabilityCheck
    shadowJarIncludeFrom(['core', 'core-api', 'minecraft', 'minecraft-api', 'bungeecord-api'])
    archiveClassifier.set('plugin')
}