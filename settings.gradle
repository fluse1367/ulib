rootProject.name = 'ulib-root'
files('loader', 'core', 'minecraft', 'velocity', 'bungeecord').each {
    if (it.directory && new File(it, 'build.gradle').exists()) {
        include ":${it.name}"
    }
}