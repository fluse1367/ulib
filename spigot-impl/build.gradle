//file:noinspection GroovyAssignabilityCheck
dependencySpigot()

dependencies {
    def projApi = project(':spigot-api')
    api projApi
    api project(':minecraft-impl')

    compileOnly projApi.ext.coordsXSeries
}

extraJavaModuleInfo {
    automaticModule('XSeries-8.6.0.0.1.jar', 'xseries')
}